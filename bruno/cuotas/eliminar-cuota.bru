meta {
  name: Eliminar Cuota
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/cuotas/{{cuotaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("elimina con 200", function() {
    expect(res.status).to.equal(200);
  });
}
