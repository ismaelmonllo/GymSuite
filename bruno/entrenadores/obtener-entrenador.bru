meta {
  name: Obtener Entrenador
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/entrenadores/{{entrenadorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200 o 404", function() {
    expect([200, 400, 404]).to.include(res.status);
  });
}
