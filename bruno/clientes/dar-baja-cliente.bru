meta {
  name: Dar Baja Cliente
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/clientes/{{clienteId}}/baja
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("da de baja con 200", function() {
    expect(res.status).to.equal(200);
  });
}
