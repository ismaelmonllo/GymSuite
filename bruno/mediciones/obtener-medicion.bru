meta {
  name: Obtener Medición
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/mediciones/{{medicionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenEntrenador}}
}

tests {
  test("responde 200 o 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
