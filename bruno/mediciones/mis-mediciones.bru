meta {
  name: Mis Mediciones (cliente)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/mediciones
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
