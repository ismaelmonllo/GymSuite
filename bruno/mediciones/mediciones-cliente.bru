meta {
  name: Mediciones de un Cliente
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/mediciones/cliente/{{clienteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenEntrenador}}
}

tests {
  test("responde 200 o 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
