meta {
  name: Mis Pagos (cliente)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/pagos/mis-pagos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

tests {
  test("responde 200 o 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
