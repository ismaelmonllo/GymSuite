meta {
  name: Generar Pagos del Mes (cron)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/pagos/generar-cron
  body: none
  auth: none
}

headers {
  x-cron-secret: {{cronSecret}}
}

tests {
  test("genera o confirma con 200 o 201", function() {
    expect([200, 201]).to.include(res.status);
  });
}
