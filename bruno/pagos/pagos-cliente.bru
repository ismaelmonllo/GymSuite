meta {
  name: Pagos de un Cliente
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/pagos/cliente/{{clienteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200 o 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
