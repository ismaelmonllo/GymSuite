meta {
  name: Stats Pagos Confirmados del Mes
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/stats/mes-pagados
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responde 200", function() {
    expect(res.status).to.equal(200);
  });
}
