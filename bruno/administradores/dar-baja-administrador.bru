meta {
  name: Dar Baja Administrador
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/administradores/{{adminId}}/baja
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("da de baja con 200", function() {
    expect(res.status).to.equal(200);
  });
}
